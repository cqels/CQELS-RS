name = "order-by-limit"
description = """
SELECT … ORDER BY DESC(?temp) LIMIT 2 over a small TRIPLES
window. SPARQL-correct expected: the two largest ?temp values,
in descending order.

**Current status (2026-07-11):** both cqels-rs and the local
cqels-java alpha.10 runner produce the expected top-2 descending rows.
This fixture now guards the SPARQL 1.1 `ORDER BY DESC(?var)` form and
LIMIT ordering behavior on both sides.
"""
ground_truth = "hand-spec"
drain_timeout_ms = 500
