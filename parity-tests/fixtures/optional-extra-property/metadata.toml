name = "optional-extra-property"
description = """
SPARQL 1.1 OPTIONAL inside a STREAM block. Each sensor reading
arrives with a required `?temp`; an optional second triple per
sensor carries `?room`. SPARQL-correct expected: one binding per
distinct ?sensor, with ?room present iff the OPTIONAL match
succeeded.

**Current status (2026-07-11):**

- **cqels-rs:** passes. STREAM-scoped OPTIONAL is parsed and evaluated
  against the window batch, yielding the SPARQL-correct 3 bindings.
- **cqels-java alpha.10 local runner:** hard-errors with the Java parser's
  "unsupported construct inside a STREAM / WINDOW / GRAPH block" diagnostic.

This fixture is now a Rust regression gate and a documented Java/Rust surface
difference, not a known Rust parser gap.
"""
ground_truth = "hand-spec"
drain_timeout_ms = 500
