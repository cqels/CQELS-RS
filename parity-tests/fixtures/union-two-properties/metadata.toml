name = "union-two-properties"
description = """
SPARQL 1.1 UNION of two alternative property paths inside a
STREAM block. SPARQL-correct expected: one row per matching
triple, with ?label bound to whichever predicate fired.

**Current status (2026-07-11):**

- **cqels-rs:** passes. STREAM-scoped UNION is parsed and evaluated
  against the window batch, yielding the SPARQL-correct 4 bindings.
- **cqels-java alpha.10 local runner:** hard-errors with the Java parser's
  "unsupported construct inside a STREAM / WINDOW / GRAPH block" diagnostic.

This fixture is now a Rust regression gate and a documented Java/Rust surface
difference, not a known Rust parser gap.
"""
ground_truth = "hand-spec"
drain_timeout_ms = 500
