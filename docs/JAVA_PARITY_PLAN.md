# Java Parity Implementation Plan

**Reference:** `github.com/cqels/claude` (Java) `v2.0.0-alpha.10` @ `d246ade186bfb42fe5da43351bad973343d19411` (2026-07-11).
**Goal:** Close the algorithmic / language-feature gaps between cqels-rs and cqels-java identified in the audit. Track progress per phase.

**Alpha.10 note:** Rust now covers Java alpha.9/alpha.10's agent-facing MCP stream-ingest and continuous-reasoning workflow (`create_stream`, `push_stream_events`, `validate_stream_query`, `watch_invariant`, `register_rules`, `CQELS_MCP_REASONING`, initialize instructions, live namespaces/status/docs resources). Java-compatible `CQELS_MCP_RDF_STORE_PATH` is accepted as an alias root for the Rust sled-backed MCP memory store; Rust writes sled data under `<path>/cqels-mcp-memory`, persists MCP memory records and procedure definitions, but is not RDF4J NativeStore and does not persist pushed stream events. Rust now has a Java-style `GraphStreamElement` for CQELS-QL/MCP graph observations: multi-statement RDF Messages are pushed as one graph element, `[TRIPLES n]` counts the observation as one stream element, and CQELS-QL matching expands the graph statements. `push_stream_events` also enforces Java alpha.10's main DoS and event-time hardening caps. Remaining alpha.10 gaps are tracked in `docs/JAVA_ALPHA10_COMPARATIVE_ANALYSIS.md`.

## Methodology

Per the project's behavioral guidelines:

- **Goal-Driven:** Each task lists a *verify* check. Move on only when the check passes.
- **Surgical:** Each PR touches one feature; no drive-by refactors of unrelated code.
- **Simplicity:** Port the algorithm, not the framework. Skip Java-isms (reflection, complex builder ceremonies) when a leaner Rust equivalent suffices.
- **Pre-commit MANDATORY** (from project CLAUDE.md):
  1. `~/.cargo/bin/cargo fmt --all`
  2. `~/.cargo/bin/cargo clippy --workspace --all-targets -- -D warnings`
  3. `~/.cargo/bin/cargo test --workspace`
  4. `RUSTDOCFLAGS="-Dwarnings" ~/.cargo/bin/cargo doc --workspace --no-deps`

## Status legend

- [ ] not started
- [~] in progress
- [x] done
- [!] blocked / needs decision

## Phase 1 — Core correctness gaps

### 1.1 MinusOperator (SPARQL 1.1 anti-join)

- [x] Port `MinusOperator` from `cqels-java/cqels-core/src/main/java/org/cqels/operator/minus/`.
- **Semantics:** For `Ω1 MINUS Ω2`, keep μ1 iff there is no μ2 compatible with μ1 and `dom(μ1) ∩ dom(μ2) ≠ ∅`.
- **Verify:** 14 parity tests in [cqels-core/src/operator/minus.rs](../cqels-core/src/operator/minus.rs) mirroring Java's `MinusOperatorTest`; pre-commit suite green.

### 1.2 Named windows + implicit stream binding (Java PR #31)

**Status note:** Java has parser + AST for `FROM NAMED WINDOW` but execution is unimplemented — `CqelsQueryCompiler` throws `UnsupportedOperationException`, tracked in Java issue #39 (commit `3afb720`). Porting Java's incomplete parser is busywork until Java itself executes named-window queries. **Deferring Phase 1.2b until upstream completes Phase 3.**

- [x] **1.2a Implicit stream binding** (Java Phase 1, complete and working): bare triple patterns in `WHERE` auto-bind to a single `FROM STREAM` when no `STREAM`/`WINDOW` blocks are present.
  - **Verify:** [cqels-core/src/parser/cqelsql.rs](../cqels-core/src/parser/cqelsql.rs) — `apply_implicit_stream_binding`; 6 new parser parity tests covering single-stream binding, multi-triple binding, explicit-block bypass, multi-stream skip, static-graph skip, named-graph skip.
- [x] **1.2b Named window parsing** (`FROM NAMED WINDOW :name ON STREAM <name> [<spec>]` + `WINDOW :name { ... }`): pest grammar extended with `from_named_window` and `window_pattern_group` rules; new `NamedWindowDefinition` AST type carries `(iri, stream, window_spec)`; new `CqelsPatternGroup::Window { window_iri, patterns }` variant. `CqelsQueryDefinition.named_windows` lists the declarations. The window-form rule is tried ahead of the bare `FROM NAMED <iri>` (static graph) form so prefix overlap doesn't shadow it. 7 tests cover declaration registration, `WINDOW {}` pattern groups, prefix-overlap with static `FROM NAMED`, multiple windows with different specs, TRIPLES specs, and error cases.
- [x] **1.2c Named window compile-time lowering** (`compiler::named_window`): a thin pass at the top of `compile_with_store` rewrites `named_windows` + `Window {}` pattern groups into ordinary `streams` + `Stream {}` groups so the rest of the compiler / planner / engine sees only the existing constructs. Same-stream + same-spec windows collapse idempotently; `WINDOW {}` references to undeclared IRIs are rejected. Recurses into `OPTIONAL` / `UNION` groups so nested windows are handled.
- [x] **1.2d Aliased stream views in the runtime** (lifts the diff-spec restriction from 1.2c): added `source_stream: Option<String>` to `CqelsStreamDefinition`, with constructors `::root` and `::aliased`. The lowering pass generates `__nw__<sanitized_iri>` synthetic entries for named windows that conflict on (source, spec); same-(source, spec) windows still share a single entry. New `ContinuousQuery::input_stream_aliases()` trait method surfaces the (synthetic, source) pairs to the engine, and `ReactiveStreamEngine::build_query_inputs_with_aliases` taps the source stream's broadcast under each synthetic name so each view sees the same elements.
- [x] **1.2e Per-stream windowing in `compiled.execute`** (lifts the long-standing "limitation #5"): factored `apply_window_spec(input, spec)` helper out of the inline window-spec match, then refactored `CompiledCqelsQuery::execute` to apply *each* stream's window spec to its own input *before* merging the resulting batches via `select_all`. Together with 1.2d, this means a named-window query with `:w1 [RANGE 10s]` and `:w2 [RANGE 30s]` on the same source now produces 10-second batches for `:w1`'s pattern group and 30-second batches for `:w2`'s, both fed by the same user-registered source.
- [x] **1.2f Source-tagged batches with per-stream pattern filtering** (closes the last cross-stream leakage gap from 1.2e): batches now flow as `(source_name, Vec<StreamElement>)` instead of bare `Vec<StreamElement>`. The matcher builds a `patterns_by_source: HashMap<String, Vec<TriplePattern>>` from `stream_patterns` (aggregating across pattern groups that share a source — e.g., self-joins) and, for each batch, restricts the patterns it tries to those declared for the batch's source plus the unscoped `default_patterns`. The `require_all_patterns` flag is gone: every selected pattern is now mandatory because we no longer mix patterns across sources. New `SourceTaggedBatch` / `SourceTaggedBatchStream` type aliases keep the multi-stream construction site readable. One new test, `source_tagged_batches_keep_pattern_matches_per_source`, exercises a two-stream query where each stream carries distinct predicates: `?v` only binds via the `s_temp` batch, `?p` only via `s_pressure`, never both, never neither.

### 1.3 Declarative CEP via FILTER(SEQ()) (Java PR #36)

- [x] Add `SEQ(...)` built-in recognized inside `FILTER` (grammar + AST).
- [x] Compile `FILTER(SEQ(...))` AST nodes to existing `cqels-engine` NFA pipeline.
- **Verify:** 7 parser parity tests + 9 `CepPatternCompiler` tests covering ordering, quantifiers, negation, alias, and window propagation; end-to-end tests run the compiled `Pattern` through `NfaPatternProcessor` over a stream of `RdfStreamElement`s.
- [x] **Single-event + cross-event FILTER predicates** wired into the compiler via `cqels-core::expression::ExpressionEvaluator`. `classify_filters` walks WHERE-level `Filter` groups, parses each expression, classifies by the set of event variables it references, and attaches single-event filters as `where_cond` on that event's stage or cross-event filters as `where_context` on the latest referenced stage. `bind_event_variables` constructs a `BindingSet` from the matched stream element using the event's triple patterns. 4 new e2e tests (single-event keep/drop + cross-event keep/drop) and 1 unit test for variable collection.

## Phase 2 — Windowing maturity

### 2.1 Triggers and evictors

- [x] Trigger types: `EventTimeTrigger`, `ProcessingTimeTrigger`, `CountTrigger`, `PurgingTrigger`, `DeltaTrigger`, `ProcessingTimeoutTrigger`, `ContinuousEventTimeTrigger`, `ContinuousProcessingTimeTrigger` (8/8 ported).
- [x] Evictor types: `CountEvictor`, `TimeEvictor`, `DeltaEvictor` (3/3 ported).
- [x] Bridge integration via `TriggerableWindowProcessor` in `cqels-core::windowing::processor` — buffers elements and consults a trigger/evictor per-window. Existing `window::Window<T>` operators continue to work unchanged.
- [x] **`Window<T>` adapter** — `TriggerableTimeWindow<T, Tr, Ev>` (tumbling), `TriggerableSlidingWindow<T, Tr, Ev>`, and `TriggerableSessionWindow<T, Tr, Ev>` in `cqels-core::windowing` all implement the existing `Window<T>` trait by delegating emission to a caller-supplied trigger + optional evictor. Tumbling/sliding use per-window `TriggerableWindowProcessor`; session windows manage state inline because their bounds grow with the data. **2.1 fully covered.**
- **Verify:** 33 framework tests + 5 adapter tests across `cqels-core/src/windowing.rs`, `windowing/processor.rs`, `windowing/delta.rs`, `windowing/timeout.rs`, `windowing/continuous.rs`, `windowing/triggerable_window.rs`.

### 2.2 Windowed self-join with indexed hash join (Java PR #25)

- [x] Indexed hash-join state for self-join over a single window (`WindowedSelfJoinState<T, K>` in `cqels-core::operator::join`).
- [x] Compile-time detection of self-join AST patterns (`detect_self_joins` in `cqels-core::compiler::self_join`). Reports source, shared variables, and pattern indices as `SelfJoinHint`s.
- [x] Compile-time hint flow into `CompiledCqelsQuery::self_join_hints()`. `CqelsQueryCompiler::compile` now invokes `detect_self_joins` during compilation and stores the resulting hints on the compiled artifact. `has_self_join_optimization()` is the runtime decision point.
- [x] **Runtime fast path** in `CompiledCqelsQuery::execute` (`try_self_join_fast_path`). When the query shape matches a strict template (1 hint, 1 shared variable in object position, no aggregates / order-by / limit / distinct / filters / binds), execution routes through `WindowedSelfJoinState` (O(N+M)) instead of the default pattern matcher (O(N·M)). Any other query shape falls through to the existing pipeline unchanged.
- [x] **Generalized fast path** (PR #24): subject-position joins (`?shared <p> ?bound`) in addition to object-position; FILTER predicates over `{left, right, shared}` variables evaluated inline before emission. Mixed-position and cross-predicate joins still fall through to the generic pipeline.
- **Verify:** 6 operator tests + 7 detection tests + 5 end-to-end fast-path tests.

## Phase 3 — Performance + persistence

### 3a Parallel hash-join (Java PR `ParallelHashJoinOperator`)

- [x] `ParallelHashJoinOperator<L, R, K, Out>` in `cqels-core::operator::parallel_hash_join`. Sequential build phase materializes the left side into `HashMap<K, Vec<L>>`; concurrent probe phase via `futures::stream::buffer_unordered`.
- [x] `ParallelWindowedAggregateOperator` in `cqels-core::operator::parallel_aggregate` — hash-partitioned parallel windowed aggregation; partitions per-window batches by group key and processes partitions concurrently. 5 tests.
- **Verify:** 6 hash-join tests + 5 aggregate tests — basic join/aggregation, empty inputs, one-to-many, parallelism invariance over {1, 2, 4, 8}, equivalence vs sequential baseline.
- [x] **SWAG CTrie index** in `cqels-core::operator::swag_ctrie` — `TimeIndex<P>` (time-bucketed range index with bag semantics) + `CTrieIndex<K, P>` (fixed-depth trie nesting one `TimeIndex` per leaf, with wildcard partial-key queries). Ported from Java's `org.cqels.operator.aggregate.swag.join.{TimeIndex, CTrieIndex}`. Single-threaded (`BTreeMap` rather than `ConcurrentSkipListMap`) and typed-keys (`K` generic rather than raw `Object`). 18 tests + 1 doctest.
- [x] **SWAG join primitives — layer 1** in `cqels-core::operator::swag_join`: `KeyIndexedTimeIndex<K, P>` (one `TimeIndex` per equi-join key + `IndexStats`), `JoinRecord<E, P>` wrapper, `KeyExtractor` trait alias with `identity()`/`constant()` helpers, and `VariableOrder` / `VariableOrderMethod` value types. 16 tests + 2 doctests. Builds on PR #32; consumed by 3a-layer-2.
- [x] **SWAG pairwise join — layer 2** in `cqels-core::operator::swag_join_state`: `JoinAggregateSpec` trait (ring-algebra contract: payload extraction, zero, add, negate, multiply, per-side payload zero/add, name) + `IntervalBounds` value type + `WindowJoinState<S, K>` stateful operator. Two indexing modes (cross-product via `TimeIndex`, equi-join via `KeyIndexedTimeIndex`); every `insert_*` / `evict_*` returns the F-IVM delta and folds it into a running aggregate. 9 tests + 1 doctest. Java's `WindowJoinState.recomputeParallel(...)` (Tier-2 batch recompute) is deferred to a later slice — it depends on the full planner.
- [x] **SWAG join planner — layer 3** in `cqels-core::operator::swag_join_graph`: `JoinGraph` (declarative topology — relations + edges with temporal windows + adjacency), DFS-based undirected acyclicity + GYO hypergraph acyclicity, `Hypergraph`/`Hyperedge` types with primal-graph construction, `build_variable_order()` (GYO join-tree DFS for acyclic, greedy min-fill for cyclic), plus `FactorizedViewPlan`/`ViewLayer`. Pure-metadata graph — key/payload extractor closures live with the Layer-4 executor. 19 tests + 1 doctest.
- [x] **SWAG join specs + windowed aggregator — layer 4 (pairwise half)** in `cqels-core::operator::swag_join_specs`: 4 concrete `JoinAggregateSpec` impls (`CountSpec`, `SumLeftSpec`, `SumRightSpec`, `SumProductSpec`) + `F64Bits` newtype for hash-stable f64 payloads + `WindowedJoinAggregator` streaming runner driven by `JoinUpdate<L, R>` events (`InsertLeft` / `InsertRight` / `EvictLeft` / `EvictRight`), emitting `JoinAggregateEvent<V>` per processed update. 8 tests + 1 doctest. Pairwise binary join is now end-to-end usable.
- [x] **SWAG multi-way join — layer 5a** in `cqels-core::operator::swag_multiway`: `MultiWayAggregateSpec` trait (generalized ring contract for N-way aggregation) + built-in `MultiWayCount` and `MultiWaySumProduct` specs + `MultiWayJoinState<E, K, S>` recursive F-IVM executor over a `JoinGraph`. Per-edge `CTrieIndex` per `(relation, neighbor)` pair; `insert` returns the F-IVM positive delta, `delete` returns the negation, both fold into the running aggregate. 10 tests covering binary / triangle / star / SumProduct / clear / validation. **Diverges from Java's `computeBinaryDelta` shortcut**: Java uses binary delta whenever the inserted relation has degree 1, which yields incorrect partial deltas for multi-way star topologies. The Rust port gates the binary fast path on `graph.relation_names().len() == 2` instead, preserving F-IVM semantics.
- [x] **SWAG triangle spec — layer 5b** in `cqels-core::operator::swag_triangle`: `TriangleJoinAggregateSpec` trait (3-way variant with type-safe per-relation payload types `PA`, `PB`, `PC`) + `TriangleCount<A, B, C>` + `TriangleSumProduct<A, B, C, FA, FB, FC>` built-in specs. Mirrors Java's `TriangleJoinAggregateSpec`. The trait is type-safe convenience surface; native execution falls through to `MultiWayJoinState` via the shared-payload path (triangle topology already runs cleanly there, as proven by Layer 5a's `triangle_multiway_count_matches_three_way_join` test). 10 tests + 1 doctest.
- [x] **SWAG multi-way range cache — layer 5c** in `cqels-core::operator::swag_multiway_cache`: bounded `RangeCache<K, E, P>` keyed by `(relation, driver, join_key, start, end)`, with `get` / `put` / `invalidate_relation` / `clear`. Memoizes per-edge range-query results during `MultiWayJoinState::recursive_join`; eagerly invalidated by relation on every `insert`/`delete` so the cache never shows stale data. FIFO eviction (vs Java's access-ordered LRU) — same hit-rate within a single delta computation, no new dependency. Default capacity 1024 entries, matching Java. 9 cache-unit tests + 3 integration tests on `MultiWayJoinState`.
- [x] **SWAG parallel join aggregator — layer 5e** in `cqels-core::operator::swag_parallel`: `ParallelJoinAggregator<S>` that takes two payload `TimeIndex`es + interval bounds and recomputes the join aggregate from scratch in parallel using rayon's work-stealing scheduler. Sequential fallback below a configurable threshold (default 1000, matching Java). Pairs with new `WindowJoinState::{left_time_index, right_time_index}` accessors (cross-product mode only). 5 tests including incremental-vs-parallel equivalence on workloads above and below the parallel threshold. Java uses Fork/Join + a hand-rolled `ExponentialSplitter`; rayon's scheduler subsumes the splitter so the `growthFactor` knob is absent here.
- [x] **SWAG windowed join operator — layer 5d** in `cqels-core::operator::swag_windowed_join`: `WindowedJoinAggregateOperator<S, K, W>` that owns per-`(join_key, window_id)` `WindowJoinState` partitions. `process_left` / `process_right` route events through a pluggable `WindowAssigner<W>`, returning one `JoinAggregateResult` per assigned window. Lifecycle methods: `fire_window(key, window)` reads the running aggregate, `purge_window(key, window)` drops the partition, `clear()` empties all state. Ships `TumblingWindowAssigner` as a canonical impl; sliding/session/custom assigners plug in via the trait without touching the operator. 9 module tests + 1 doctest. Java's Reactor `Flux`/`Mono` envelope is replaced by synchronous methods returning `Vec<Result>` — callers wrap on their own async story if needed.
- [x] **SWAG multi-way `delete_range` — layer 5f** in `cqels-core::operator::swag_multiway`: `MultiWayJoinState::delete_range(relation, start, end) -> V` enumerates every `(timestamp, element)` occurrence in `[start, end]` via the relation's per-edge `CTrieIndex` (which stores full `JoinRecord<E, P>`), then replays each through the regular `delete` path to recover the F-IVM negative delta. 4 tests covering empty-range / no-match / bag multiplicity / triangle topology end-to-end. **Surpasses Java's implementation**, which itself notes `deleteRange` returns zero delta because Java's `TimeIndex` only stores payloads. The Rust port keeps full elements in `JoinRecord` and so closes that gap.

**3a complete.** All five SWAG-join layers + their five sub-slices (5a–5f) are landed. The full F-IVM pipeline — pairwise and N-way, with topology planner + range cache + parallel recompute + windowed key partitioning — is in `cqels-core::operator`.

### 3b Persistent storage backends

- [x] **`cqels-storage-sled`** — production-grade embedded backend using the pure-Rust `sled` KV store. 8 tests.
- [x] **`cqels-storage-lmdb`** — production-grade embedded backend using the pure-Rust `heed` LMDB binding (memory-mapped, MVCC). 8 tests.
- [x] **`cqels-storage-rocksdb`** — RocksDB backend (mirrors Java's `cqels-storage-rocksdb`). The previously-blocking `links = "rocksdb"` collision between the user-space `rocksdb` crate and oxigraph's transitive `oxrocksdb-sys` was lifted by disabling oxigraph's `rocksdb` *default* feature at the workspace level: we only use `Store::new()` (the in-memory variant available unconditionally), never `Store::open(path)` (the rocksdb-gated API). Column-family layout: `journal`, `checkpoints`, `meta`. Uses `delete_range_cf` for cheap range truncation. 8 tests, same shape as the sled backend.
- [x] **`cqels-storage-iotdb`** — Apache IoTDB-backed adapter (mirrors Java's `cqels-storage-iotdb`). Lifts the "network database, large surface, no concrete driver" deferral by splitting the integration: a narrow async `IotDbExecutor` trait (`put`, `read_from`, `latest`, `delete_range`, `close`) captures exactly what the SPI needs, and a full-functioning `InMemoryIotDbExecutor` (backed by per-device `BTreeMap<u64, Vec<u8>>`) ships as the default backend. Storage maps `offset → IoTDB time`, journal device + checkpoint device + reserved key-0 meta row for next-offset recovery across reopens. 14 unit tests (executor-level + backend-level) all green.
  - **`thrift` feature** (opt-in): `ThriftIotDbExecutor` built on `iotdb-client-rs::RpcSession`. Because the Thrift `Session` carries `dyn TInputProtocol / TOutputProtocol` trait objects without `Send + Sync`, the session is pinned to a dedicated OS worker thread; async methods forward commands over `tokio::sync::mpsc` channels with one-shot reply slots. The executor handle is `Send + Sync` (just an mpsc sender). Payload bytes encode as base64 in a single TEXT measurement per device because the client's `Value` enum has no Binary variant. Live integration test gated by `IOTDB_HOST` env var + `#[ignore]`; plus a compile-time type-plumbing test and a `connect_to_dead_endpoint_yields_clean_error` test that doesn't need a live server.

### 3c External integration modules

- [x] **`cqels-mcp` crate** — protocol-agnostic tool + prompt + resource layer, JSON-RPC 2.0 transport over stdio and opt-in HTTP, and executable server binary. Default stdio/HTTP tool surface now includes Java alpha.10's documented MCP handles: `store_memory`, `recall_memory`, `forget_memory`, `query` (`waitMs` accepted/clamped), `create_stream`, `push_stream_events`, `validate_stream_query`, `register_stream_query`, `forget_stream_query`, `watch_invariant`, `register_rules`, `reason`, `validate`, `solve`, `save_procedure`, `list_procedures`, `run_procedure`, `record_event`, `recall_episodes`, `explain_decision`, `recall_decisions`, `register_reasoning`, `set_access_policy`, and `assemble_context`; Rust additionally exposes `parse_query`, `analyze_query`, `reasoning_profiles`, `shacl_capabilities`, `list_stream_queries`, `unregister_stream_query`, and `poll_stream_results`. Prompt surface: Java alpha.8's eight CQELS workflow/query templates (`store_knowledge`, `recall_about`, `validate_data`, `reasoning_workflow`, `recent_events_window`, `entity_by_type`, `value_over_window`, `spatial_recall`). Resource surface: `cqels://kg/stats`, `cqels://kg/namespaces`, `cqels://engine/status`, `cqels://streams`, `cqels://queries`, `cqels://reasoning/capabilities`, `cqels://docs/cqelsql`, `cqels://docs/cep`, and `cqels://queries/{queryId}/results`. JSON-RPC methods: `initialize` (including alpha.10 agent instructions), `ping`, `tools/list`, `tools/call`, `prompts/list`, `prompts/get`, `resources/list`, `resources/templates/list`, `resources/read`, `resources/subscribe`, `resources/unsubscribe`. Notifications, parse errors, invalid JSON-RPC versions, and unknown methods all handled correctly. Run with `cargo run -p cqels-mcp --bin cqels_mcp_server` and pipe JSON-RPC on stdin, or set `CQELS_MCP_TRANSPORT=http` for the HTTP endpoint.
- [x] **Engine-bound stream-query + ingest tools** (`stream_query` module): `create_stream`, `push_stream_events`, `validate_stream_query`, `register_stream_query`, Java-compatible `forget_stream_query`, `list_stream_queries`, Rust-compatible `unregister_stream_query`, `poll_stream_results`, `watch_invariant`, and `register_rules`. The default server wires a live `StreamQueryHub` backed by `CqelsEngine`; resources use the same hub so `cqels://queries` and `cqels://queries/{queryId}/results` observe both engine query buffers and JSON observer buffers. Java options `queryId`, `bufferSize`, `notify`, and `reportConforming` are accepted; unsupported `language` values and `cep:true` fail loud rather than being silently overclaimed. RDF-message N-Quads `VERSION "1.2-messages"` + `MESSAGE` delimiters are parsed with `oxttl`; one-statement observations push as RDF elements and multi-statement observations push as graph elements. `push_stream_events` rejects empty event arrays, fractional/out-of-range event times, over-limit streams, per-message statement floods, total observation floods, per-event character floods, and total-call character floods before any push. Stream-query tests cover registration round-trip, Java caller-supplied IDs, buffer/notify options, idempotent and capped stream creation, RDF-message ingest, graph observation atomicity under `[TRIPLES n]`, Java alpha.10 ingest hardening, validation-only query checks, SHACL invariant buffering, ASP result buffering, opt-in RDFS stream reasoning, listing, Java/Rust unregister + buffer-clear, retry/double-forget behavior, parse-error propagation, exact Java error wording, schema advertising, and FIFO drain semantics.
- [x] **Java alpha.10 opt-in stream reasoning** — `CQELS_MCP_REASONING={rdfs|rdfs-full}` enables a per-stream `cqels_reasoning::ReteNetwork` inside `StreamQueryHub`. Original pushed observations flow first; inferred triples are pushed after them with the same event timestamp and are also visible to MCP-level observers. Default is off, matching Java's deployment posture. 2 tests cover config parsing and RDFS subclass inference.
- [x] **Java alpha.10 persistence/env compatibility** — Rust continues to use the existing `MemoryStore` abstraction for MCP memory records and procedure definitions, with `SledMemoryStore` enabled directly by `CQELS_MCP_MEMORY_DIR`. `CQELS_MCP_RDF_STORE_PATH` is now accepted as a Java-compatible alias root so alpha.10 launcher configs do not silently fall back to memory-only behavior; Rust stores sled data under `<path>/cqels-mcp-memory` instead of mixing files into a Java RDF4J NativeStore root. This alias does not turn Rust's live stream events into durable RDF4J quads.
- [x] **Java alpha.8 cognitive-memory tools** — `save_procedure`/`list_procedures`/`run_procedure`, `record_event`/`recall_episodes`, `explain_decision`/`recall_decisions`, `set_access_policy`, and `assemble_context` are backed by the same in-memory or sled `MemoryStore` used by `store_memory`. Procedure runs perform placeholder binding and dry-run dispatch to the matching Rust surface, recording decision-lineage traces when decision context is supplied; episodic and decision recall support time/entity/policy filters; governance role filters pattern recall by statement `accessLabel`; working-memory assembly provides lexical GraphRAG-style bundles with vector/hybrid modes failing loud when no vector search is configured.
- [x] **MCP resources + query-results template** (`resource` module): Java alpha.10 metadata/docs resources are exposed through `resources/list` and `resources/read`: `cqels://kg/stats`, `cqels://kg/namespaces`, `cqels://engine/status`, `cqels://streams`, `cqels://queries`, `cqels://reasoning/capabilities`, `cqels://docs/cqelsql`, `cqels://docs/cep`; `cqels://queries/{queryId}/results` drains the same `StreamQueryHub` buffer used by `poll_stream_results`. The transport advertises `resources.subscribe`, acknowledges subscribe/unsubscribe for known URIs, and exposes canonical `notifications/resources/updated` payload helpers for hosts that push query-result signals.
- [x] **MCP HTTP transport parity slice (Java alpha.8)** — `CQELS_MCP_TRANSPORT=http` binds the same JSON-RPC surface to `127.0.0.1:3000/mcp` by default, with `CQELS_MCP_HTTP_{HOST,PORT,PATH}` overrides, optional bearer auth, Origin/Host allow-lists, unauthenticated `/health`, exact-path validation, and a 1 MiB request-body cap (`CQELS_MCP_HTTP_MAX_BODY_BYTES`, `<= 0` disables). Mirrors the default-off deployment posture of Java `v2.0.0-alpha.8` while reusing the existing Rust dispatcher.
- [x] **`validate` MCP tool** (`validate` module): bridges `cqels-shacl::ShaclValidationEngine` to the MCP surface. Takes `shapes` + `data` triple arrays, runs SHACL validation through an `AspSolver`, and returns structured violations plus optional repair candidates. Default constructor uses `ClingoSubprocessSolver` (clingo-conditional); a `validate_tool_with_solver` constructor accepts any `Arc<dyn AspSolver>` for testing or custom backends. 8 unit tests use a recording `MockSolver` to verify conforming/violation paths, error propagation, and that data triples thread into the generated ASP program.
- [x] **`solve` MCP tool** (`solve` module): direct ASP-solver bridge. Takes a raw ASP-Core-2 `program` string + `max_models` (0 = enumerate all), returns the answer sets as `{predicate, terms}` atom arrays. Same constructor pattern as `validate`: `solve_tool()` default with `ClingoSubprocessSolver`, `solve_tool_with_solver(Arc<dyn AspSolver>)` for tests. 8 unit tests cover canned answer sets, program + max_models pass-through, default `max_models = 1`, solver-error propagation, missing-program rejection, UNSAT (empty list), and multi-model enumeration.
- [x] **`cqels-cdsp` Rust port:** COVESA Vehicle Signal Specification (VSS) signal-ingestion adapter. Provides `VssSignal` + `VssValue` (Bool/Int/Float/String) envelope, a `VssSignalMapper` that produces an `RdfStreamElement` from a signal using a configurable `PredicateStrategy` (`PrefixedPath` or `Slashed`), and an async `VssSignalSource` trait with a reference `ChannelVssSignalSource` backed by `tokio::sync::mpsc`. 16 default-feature unit tests + 1 doctest.
  - **`kuksa` feature** (opt-in): `KuksaVssSignalSource` built on `kuksa-rust-sdk`'s gRPC Databroker v2 client. Unlike the IoTDB Thrift client, kuksa-rust-sdk is fully async / `Send + Sync` (tonic-based), so no worker-thread workaround is needed — a single background tokio task drains the subscription stream into a bounded `tokio::sync::mpsc` channel that back-pressures producers when consumers fall behind. `connect()` validates the subscription synchronously (bad endpoint or unknown path surfaces immediately rather than from the first `next()`). Value mapping lifts kuksa's scalar `TypedValue` variants to the narrower `VssValue` (`Int32/Int64/Uint32`→`Int(i64)`; `Uint64` lifts only when it fits, otherwise drops; `Float/Double`→`Float(f64)`; arrays drop). 10 new unit tests + a `connect_to_dead_endpoint_yields_clean_error` test that doesn't need a server + a `kuksa_live_subscription_delivers_signals` integration test gated by `KUKSA_HOST`.

### 3d Property-based testing

- [x] `cqels-core/tests/proptest_joins.rs` — proptest property tests for `WindowedSelfJoinState` and `ParallelHashJoinOperator` over random event streams, comparing against O(N²)/O(N·M) nested-loop baselines. 3 properties × 50–100 cases each.

### 3e Cross-language parity harness

- [x] **`parity-tests/`** — a language-neutral workload corpus that lets `cqels-rs` and `cqels-java` run the same queries against the same inputs and check that their outputs agree. Each workload is a directory of `{metadata.toml, query.cqels, streams.jsonl, expected.jsonl}` files. A standalone Rust runner (`runner-rust/`, its own cargo `[workspace]` so `cargo test --workspace` at the repo root is unaffected) loads a workload, drives it through the engine, and diffs produced bindings against `expected.jsonl` (exit code 0 = match, 1 = mismatch with unified diff, 2 = engine error, 3 = load error). The current 12-fixture corpus covers `simple-select-now`, `triples-window-batch`, `range-window-low-volume`, `triples-filter-numeric`, `triples-multi-pattern-join`, `self-join-rides`, `count-aggregate`, `order-by-limit`, `stream-static-lookup-join`, `optional-extra-property`, `union-two-properties`, and `cep-sequence-two-events`.
- [x] **`cqels-benchmarks::parity_fixtures`** — criterion benchmark that auto-discovers every fixture under `parity-tests/fixtures/`, builds a `CqelsEngine`, drives the workload end-to-end (create_stream → register_cqelsql_query → push events → close_stream → drain bindings → stop), and reports throughput in events/sec. Each fixture asserts its expected binding count per iteration so a correctness regression fails the bench loudly. Today's numbers are dominated by per-iteration engine spin-up (~7 ms baseline); larger workloads will drive the workload cost above the engine cost so the trend is meaningful. Side-by-side with a Java-side criterion-equivalent (JMH) is the next slice.
- [x] **`parity-tests/runner-java/`** — Java counterpart to `runner-rust`, written against the cqels/claude public façade (`org.cqels.engine.CQELSEngine` + `DataStream` + `QueryResultListener`, RDF4J value model, JDK 17). Same fixture format, same exit codes, so CI can treat both runners interchangeably. cqels/claude is private + not on Maven Central, so consumers must clone + `mvn install -DskipTests` it once; Maven coordinates are overridable via `-Dcqels.dependency.{groupId,artifactId,version}` for forks. A JMH uber-jar (`target/parity-bench.jar`) drives the same fixtures end-to-end for side-by-side timing against `cqels-benchmarks::parity_fixtures`. Java-golden capture (replacing hand-spec `expected.jsonl` with cqels-java's actual output, setting `ground_truth = "java-golden"`) is the next slice once a maintainer with a stable cqels/claude build runs the sweep.
- [x] **CI: `nightly-parity-java` job** — `.github/workflows/ci.yml` nightly job clones cqels/claude (via the `CQELS_CLAUDE_PAT` secret), `mvn install`s it, builds `parity-tests/runner-java`, then enforces a regression gate on the 4 known-passing fixtures (`simple-select-now`, `range-window-low-volume`, `triples-filter-numeric`, `triples-window-batch`) and runs the full `--all` sweep informationally (uploaded as `parity-java-log` artifact for visibility). A fresh local Java sweep on 2026-07-11 passed 6/12 fixtures against the current oracles; the CI gate can be widened after intentional Java/Rust semantic differences are represented with comparison modes or separate oracles. Job exits 0 early with a `::notice::` if the secret is unset — keeps the workflow green for forks without access to cqels/claude.

## Out of scope (intentional)

- Porting Java's reflection-heavy `BinaryCodecRegistry` generic dispatch — the byte-based Rust SPI is fine for the backends we'll add.
- Re-introducing the separate `cqels-reasoning-rete` crate — folded `cqels-reasoning` is simpler.
- Java's mislabeled `cqels-s2` (which is Lucene semantic search, not S2 cells).
- Java alpha.10's JVM deploy-time extension mechanisms (`cqels-plugin-spi`, `ServiceLoader` plugin discovery, and `ServiceLoader` embedding-provider discovery). Rust exposes native extension points (`ToolRegistry`, `MemoryStore`, solver injection, and resource registries) instead of loading Java jars or classpath providers.

## Done log

- **1.1 MinusOperator** — `cqels-core/src/operator/minus.rs`, 14 parity tests; SPARQL 1.1 §8.3 compatibility, disjoint-domain anti-join semantics. Skipped Java's `Builder` and `Duration` timeout (not needed in Rust idiom).
- **1.2a Implicit stream binding** — `apply_implicit_stream_binding` in `cqels-core/src/parser/cqelsql.rs`. Bare triple patterns auto-bind to the single FROM STREAM when no explicit STREAM block / multiple streams / static or named graphs are present. 6 parser parity tests; updated existing `test_parse_rdf_type_shorthand`. Also exposed `streams()` / `static_graphs()` / `named_graphs()` accessors on the builder.
- **1.3 Declarative CEP via FILTER(SEQ())** — `SeqConstraint`/`SeqArg` AST in `cqels-core::parser::ast`, pest grammar rules (`seq_call`, `seq_arg`, `seq_quantifier`, `seq_not_kw`), parser logic in `parse_seq_call`/`parse_seq_arg`, and `CepPatternCompiler` in `cqels-engine::cep_compiler` that maps SEQ to `Pattern<RdfStreamElement>`. 7 parser tests + 9 compiler tests including 2 end-to-end through `NfaPatternProcessor`. Added `Pattern::previous()` accessor for chain introspection. Not yet ported: single-event/cross-event FILTER predicate evaluation (needs expression evaluator wiring).
- **2.1 Triggers + evictors** — new `cqels-core::windowing` module with `WindowBounds` (`TimeWindow`, `GlobalWindow`), `TriggerResult`, `TriggerContext`, `Trigger` trait, `Evictor` trait, and concrete `EventTimeTrigger`/`ProcessingTimeTrigger`/`CountTrigger`/`PurgingTrigger`/`CountEvictor`/`TimeEvictor`. 17 parity tests. Triggers hold inline state instead of Java's framework-managed partitioned state. Continuous/delta variants and integration with existing `window::Window<T>` operators are tracked as follow-ups.
- **2.2 Windowed self-join with indexed hash** — `WindowedSelfJoinState<T, K>` in `cqels-core::operator::join` with `SelfJoinPair<T>` result type. Hash index keyed by caller-supplied join key, time-ordered per-key buckets, watermark-driven eviction. 6 parity tests including an O(N²) baseline equivalence check. Compile-time detection (rewriting self-join AST patterns to use this operator) is deferred — the operator is exposed for direct construction.
- **1.3 follow-up: SEQ FILTER predicates wired** — `classify_filters` + `bind_event_variables` + `term_to_value` in `cqels-engine::cep_compiler` route single-event filters into per-state `where_cond` and cross-event filters into `where_context` at the latest referenced state. Uses the existing `cqels-core::expression::ExpressionEvaluator`. 5 new tests bring the cep_compiler suite to 14.
- **2.2 follow-up: compile-time self-join detection** — new `cqels-core::compiler::self_join` module with `SelfJoinHint` and `detect_self_joins(query_def)`. Inspects WHERE-level Stream pattern groups, reports pairs sharing source + at least one variable. 7 tests.
- **2.1 follow-up: TriggerableWindowProcessor** — new `cqels-core::windowing::processor` bridges trigger/evictor framework to a stateful per-window stream processor. 5 tests.
- **2.1 follow-up: Delta + Timeout triggers/evictors** — `DeltaFunction`, `DeltaTrigger`, `DeltaEvictor` (`windowing::delta`); `ProcessingTimeoutTrigger` (`windowing::timeout`). 11 tests.
- **2.1 follow-up: Continuous trigger variants** — `ContinuousEventTimeTrigger`, `ContinuousProcessingTimeTrigger` (`windowing::continuous`). 7 tests. Trigger family now 8/8 ported.
- **3a Parallel hash-join** — `ParallelHashJoinOperator<L, R, K, Out>` in `cqels-core::operator::parallel_hash_join`. Sequential build → concurrent probe via `buffer_unordered`. 6 tests including a baseline equivalence over 50×30 elements.
- **3b cqels-storage-sled** — first production-grade backend. Implements `PersistentBackend`/`EventJournal`/`CheckpointStore`/`StorageBackendProvider`. Sled trees keyed by 8-byte big-endian offsets/IDs, JSON-encoded payloads, recoverable next-offset counter. 8 tests including reopen-and-resume.
- **3b cqels-storage-lmdb** — second production-grade backend using `heed` (pure-Rust LMDB binding). Memory-mapped, MVCC, copy-on-write transactions. Three LMDB databases (`journal`, `checkpoints`, `meta`) in a single env. 8 tests, identical surface to sled.
- **3c cqels-mcp scaffolding** — `McpTool` trait + `ToolRegistry` + `ToolInputSchema`/`ToolInvocation`/`ToolResult` value types. Reference tools `parse_query` and `query` (dry-run). Transport-agnostic — any Rust MCP SDK can wrap the registry.
- **3c cqels-mcp resources** — `ResourceRegistry` + descriptors + JSON resource content types; four alpha.8 metadata resources and the `cqels://queries/{queryId}/results` resource template; JSON-RPC `resources/list`, `resources/templates/list`, `resources/read`, `resources/subscribe`, `resources/unsubscribe`; canonical `notifications/resources/updated` helper. 80 `cqels-mcp` unit tests + stdio integration.
- **3c alpha.10 MCP pump** — `create_stream`, `push_stream_events`, `validate_stream_query`, `watch_invariant`, `register_rules`, opt-in `CQELS_MCP_REASONING`, initialize instructions, RDF-message N-Quads parsing, graph-observation ingest for CQELS-QL `[TRIPLES n]`, Java alpha.10 stream-ingest hardening, live namespaces/status/docs resources, Java-compatible `CQELS_MCP_RDF_STORE_PATH` aliasing, and query result buffers generalized from `BindingSet` to JSON values for continuous SHACL/ASP reports. `cqels-mcp` unit coverage includes the alpha.10 graph-observation and ingest-hardening paths plus stdio integration.
- **3d Property tests** — proptest comparisons of `WindowedSelfJoinState` and `ParallelHashJoinOperator` against brute-force baselines across random event streams (100/100/50 cases).
- **3a follow-up: parallel windowed-aggregate** — `ParallelWindowedAggregateOperator` in `cqels-core::operator::parallel_aggregate`. Hash-partitioned by group key; per-partition aggregation runs concurrently then merges. 5 tests.
- **2.2 follow-up: generalized self-join fast path (PR #24)** — extends `try_self_join_fast_path` to accept (a) subject-position self-joins via new `JoinKeyPosition` enum and (b) FILTER predicates over `{left_bound, right_bound, shared}` variables, evaluated inline with `ExpressionEvaluator` before emission. New `simple_join_pattern` returns the position; new `collect_variables` walks expressions to check filter scope. Mixed-position and cross-predicate joins still fall through. 2 new integration tests bring the fast-path suite to 5.
